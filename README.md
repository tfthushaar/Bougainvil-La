# Bougainvil'La

- **`site/`** — the public marketing site (Next.js, deployed to Cloudflare Workers at
  `bougainvilla.co.in`). See `site/README.md` for setup, scripts, and content model.
- **`admin/`** — the CMS/admin app (planned, not yet built — see `site/docs/netlify-admin-plan.md`
  for the design). Will deploy to Netlify, separately from `site/`.

Both live in this one repo so the same session/tooling can work across both once `admin/` exists.
